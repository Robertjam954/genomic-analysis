# ─── Path Variables ───────────────────────────────────────────────
# Copy this file to .env and fill in the values. Never commit .env.

# Root of the project (auto-detected in most scripts)
PROJECT_ROOT=/Users/robertjames/Documents/GitHub/genomic-analysis-scripts

# Private data directory (local only — never committed)
# Contains raw PHI data, deidentified outputs, extracted text
DATA_PRIVATE_DIR=/path/to/local/data_private

# ─── API Keys ─────────────────────────────────────────────────────
# Add any API keys needed for external services below
# OPENAI_API_KEY=sk-...
